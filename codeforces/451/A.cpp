#include<bits/stdc++.h>
using namespace std;
main ()
{
    int h,v;
    cin>>h>>v;
    if(h>v)
    {
        swap(h,v);
    }
    if(h%2==0)
    {
        cout<<"Malvika";
    }
    else

    cout<<"Akshat";
}
